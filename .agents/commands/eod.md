---
description: Run the eod skill with optional invocation arguments.
agent: build
---

Load the `eod` skill with the native skill tool and follow it exactly.
Treat `$ARGUMENTS` as the invocation arguments for that skill.
