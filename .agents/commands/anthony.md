---
description: Run the anthony skill with optional invocation arguments.
agent: build
---

Load the `anthony` skill with the native skill tool and follow it exactly.
Treat `$ARGUMENTS` as the invocation arguments for that skill.
