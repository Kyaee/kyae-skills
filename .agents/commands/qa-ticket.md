---
description: Run the qa-ticket skill with optional invocation arguments.
agent: build
---

Load the `qa-ticket` skill with the native skill tool and follow it exactly.
Treat `$ARGUMENTS` as the invocation arguments for that skill.
